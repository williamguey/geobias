# Pinned environment for exact reproduction of every figure and statistic.
# scipy is pinned because wilcoxon()'s zero/tie handling changed across versions,
# which shifts the per-model Wilcoxon p-values at the third decimal.
numpy==1.24.3
pandas==2.0.3
scipy==1.11.1
statsmodels==0.14.0
scikit-learn==1.3.0
matplotlib==3.7.2
openpyxl==3.1.2
python-docx==1.2.0
# gradio is only needed to run the interactive app (geobias_app.py) end to end;
# the analysis scripts import it solely for the stimulus/wrapper constants.
gradio>=4.0
